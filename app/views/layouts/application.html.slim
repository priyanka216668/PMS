doctype html
html
  head
    title
      | Pms
    = csrf_meta_tags
    = csp_meta_tag
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload'
    = javascript_include_tag 'application', 'data-turbolinks-track': 'reload'
  body
    nav.navbar.navbar-light.bg-primary
      a.navbar-brand PMS
      - if user_signed_in?
        .pull-right
          = link_to('Reports', reports_path, method: :get, class: 'btn btn-secondary btn-sm')
          = link_to('Logout', destroy_user_session_path, method: :delete, class: 'btn btn-success btn-sm')


    p.notice
      = notice
    p.alert
      = alert
    
    = yield
